#include "Lexer.h"
#include <vector>

Lexer::Lexer() {
    // Initialize the DFAs here
}

std::vector<Token> Lexer::tokenize(const std::string& input) {
    std::vector<Token> tokens;
    // Tokenize Use the DFAs to tokanise here (will probably be a lot of if statements)
    return tokens;
}